## Packages
framer-motion | Page transitions and list animations for a premium feel
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
clsx | Class merging utility
tailwind-merge | Tailwind class deduplication

## Notes
Audio recording uses the pre-existing `useVoiceRecorder` hook located in `client/replit_integrations/audio/useVoiceRecorder.ts`.
We use a top-level layout with a clean, medical-themed UI (Outfit + Plus Jakarta Sans fonts).
